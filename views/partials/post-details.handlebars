{{!-- create post cards that will show on the homepage --}}
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{title}}</h5>
    <p class="card-text">{{cutString content}}</p>
    <a href="/post/{{id}}" class="card-link">View Post</a>
  </div>
</div>